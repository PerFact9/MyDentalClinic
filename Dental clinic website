<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#1a3a4a">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>My Dental Care | Modern Dentistry</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --primary: #1a3a4a;
            --primary-light: #2d5a6b;
            --accent: #c9a96e;
            --accent-light: #e8d5a3;
            --text: #2c3e50;
            --text-light: #5a6c7d;
            --bg: #faf9f7;
            --white: #ffffff;
            --shadow: 0 4px 20px rgba(0,0,0,0.08);
            --shadow-hover: 0 12px 40px rgba(0,0,0,0.12);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-family: 'Inter', sans-serif;
            color: var(--text);
            background: var(--bg);
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        h1, h2, h3, h4 {
            font-family: 'Playfair Display', serif;
            font-weight: 500;
            line-height: 1.2;
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        button {
            font-family: inherit;
            cursor: pointer;
            border: none;
            background: none;
        }

        /* ===== NAVIGATION ===== */
        nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 1rem 5%;
            transition: all 0.3s ease;
        }

        nav.scrolled {
            background: rgba(255,255,255,0.98);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            box-shadow: 0 2px 20px rgba(0,0,0,0.06);
            padding: 0.7rem 5%;
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: clamp(1.3rem, 4vw, 1.8rem);
            font-weight: 600;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            z-index: 1002;
        }

        .logo-icon {
            width: 36px;
            height: 36px;
            min-width: 36px;
            background: var(--primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            font-weight: 500;
            font-size: 0.9rem;
            color: var(--text);
            transition: color 0.3s;
            position: relative;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent);
            transition: width 0.3s;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .mobile-menu-btn {
            display: none;
            z-index: 1002;
            color: var(--primary);
            padding: 0.5rem;
        }

        .btn-primary {
            background: var(--primary);
            color: white;
            padding: 0.75rem 1.8rem;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s;
            border: 2px solid var(--primary);
            display: inline-block;
            text-align: center;
            font-size: 0.95rem;
        }

        .btn-primary:hover {
            background: transparent;
            color: var(--primary);
            transform: translateY(-2px);
        }

        .btn-outline {
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
            padding: 0.75rem 1.8rem;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s;
            display: inline-block;
            text-align: center;
            font-size: 0.95rem;
        }

        .btn-outline:hover {
            background: var(--primary);
            color: white;
        }

        /* ===== HERO ===== */
        .hero {
            min-height: 100vh;
            min-height: 100dvh;
            display: flex;
            align-items: center;
            padding: 7rem 5% 4rem;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -10%;
            right: -10%;
            width: 65%;
            height: 120%;
            background: linear-gradient(135deg, #e8f4f8 0%, #f0e6d3 100%);
            border-radius: 0 0 0 150px;
            z-index: -1;
        }

        .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            align-items: center;
            width: 100%;
        }

        .hero-content h1 {
            font-size: clamp(2.2rem, 5vw, 4rem);
            color: var(--primary);
            margin-bottom: 1.2rem;
        }

        .hero-content h1 span {
            color: var(--accent);
            font-style: italic;
        }

        .hero-content p {
            font-size: clamp(1rem, 2vw, 1.2rem);
            color: var(--text-light);
            margin-bottom: 2rem;
            max-width: 500px;
            line-height: 1.7;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .hero-image {
            position: relative;
        }

        .hero-image img {
            width: 100%;
            border-radius: 20px;
            box-shadow: var(--shadow-hover);
            object-fit: cover;
            aspect-ratio: 4/3;
        }

        .hero-badge {
            position: absolute;
            bottom: -15px;
            left: -15px;
            background: white;
            padding: 1rem 1.5rem;
            border-radius: 15px;
            box-shadow: var(--shadow);
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .badge-icon {
            width: 44px;
            height: 44px;
            min-width: 44px;
            background: var(--accent-light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary);
        }

        .badge-text h4 {
            font-size: 1rem;
            color: var(--primary);
        }

        .badge-text p {
            font-size: 0.8rem;
            color: var(--text-light);
            margin: 0;
        }

        /* ===== SECTIONS ===== */
        section {
            padding: clamp(3rem, 8vw, 6rem) 5%;
        }

        .section-header {
            text-align: center;
            max-width: 700px;
            margin: 0 auto clamp(2rem, 5vw, 4rem);
            padding: 0 1rem;
        }

        .section-header h2 {
            font-size: clamp(1.8rem, 4vw, 3rem);
            color: var(--primary);
            margin-bottom: 0.8rem;
        }

        .section-header p {
            color: var(--text-light);
            font-size: clamp(0.95rem, 2vw, 1.1rem);
        }

        .label {
            display: inline-block;
            padding: 0.35rem 1rem;
            background: var(--accent-light);
            color: var(--primary);
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            margin-bottom: 0.8rem;
        }

        /* ===== SERVICES ===== */
        .services {
            background: white;
        }

        .services-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .service-card {
            background: var(--bg);
            padding: clamp(1.8rem, 4vw, 2.5rem);
            border-radius: 20px;
            transition: all 0.4s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--accent);
            transform: scaleX(0);
            transition: transform 0.4s ease;
        }

        .service-card:hover::before {
            transform: scaleX(1);
        }

        .service-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        .service-icon {
            width: 56px;
            height: 56px;
            min-width: 56px;
            background: white;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.2rem;
            color: var(--primary);
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        .service-card h3 {
            font-size: clamp(1.1rem, 2.5vw, 1.3rem);
            margin-bottom: 0.6rem;
            color: var(--primary);
        }

        .service-card p {
            color: var(--text-light);
            font-size: 0.9rem;
            line-height: 1.7;
        }

        .service-link {
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            margin-top: 1.2rem;
            color: var(--primary);
            font-weight: 600;
            font-size: 0.85rem;
            transition: gap 0.3s;
        }

        .service-link:hover {
            gap: 0.8rem;
        }

        /* ===== WHY US ===== */
        .why-us {
            background: var(--primary);
            color: white;
            position: relative;
            overflow: hidden;
        }

        .why-us .section-header h2,
        .why-us .section-header p {
            color: white;
        }

        .why-us .label {
            background: rgba(255,255,255,0.15);
            color: white;
        }

        .features-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: clamp(1.5rem, 4vw, 3rem);
        }

        .feature-item {
            text-align: center;
            padding: clamp(1rem, 3vw, 2rem);
        }

        .feature-number {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 6vw, 4rem);
            color: var(--accent);
            opacity: 0.4;
            line-height: 1;
            margin-bottom: 0.8rem;
        }

        .feature-item h3 {
            font-size: clamp(1.1rem, 2.5vw, 1.3rem);
            margin-bottom: 0.6rem;
        }

        .feature-item p {
            color: rgba(255,255,255,0.7);
            font-size: 0.9rem;
            line-height: 1.7;
        }

        /* ===== GALLERY ===== */
        .gallery-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: clamp(0.8rem, 2vw, 1.5rem);
        }

        .gallery-item {
            position: relative;
            border-radius: 15px;
            overflow: hidden;
            aspect-ratio: 4/3;
            cursor: pointer;
        }

        .gallery-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .gallery-item:hover img {
            transform: scale(1.08);
        }

        .gallery-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to top, rgba(26,58,74,0.85), transparent 60%);
            display: flex;
            align-items: flex-end;
            padding: clamp(1rem, 3vw, 2rem);
            opacity: 0;
            transition: opacity 0.3s;
        }

        .gallery-item:hover .gallery-overlay {
            opacity: 1;
        }

        .gallery-overlay h4 {
            color: white;
            font-size: clamp(0.9rem, 2vw, 1.2rem);
        }

        /* ===== TESTIMONIALS ===== */
        .testimonials {
            background: white;
        }

        .testimonial-slider {
            max-width: 900px;
            margin: 0 auto;
            position: relative;
            padding: 0 1rem;
        }

        .testimonial-slide {
            text-align: center;
            padding: clamp(1rem, 3vw, 2rem);
            display: none;
        }

        .testimonial-slide.active {
            display: block;
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .quote-icon {
            color: var(--accent);
            margin-bottom: 1rem;
        }

        .testimonial-slide p {
            font-size: clamp(1.1rem, 3vw, 1.4rem);
            font-family: 'Playfair Display', serif;
            color: var(--primary);
            line-height: 1.7;
            margin-bottom: 1.5rem;
            font-style: italic;
        }

        .testimonial-author {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .author-avatar {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            object-fit: cover;
        }

        .author-info {
            text-align: left;
        }

        .author-info h4 {
            color: var(--primary);
            font-size: 1rem;
        }

        .author-info p {
            font-family: 'Inter', sans-serif;
            font-size: 0.85rem;
            color: var(--text-light);
            font-style: normal;
            margin: 0;
        }

        .slider-dots {
            display: flex;
            justify-content: center;
            gap: 0.5rem;
            margin-top: 1.5rem;
        }

        .dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: #ddd;
            cursor: pointer;
            transition: all 0.3s;
            border: none;
            padding: 0;
        }

        .dot.active {
            background: var(--accent);
            width: 28px;
            border-radius: 5px;
        }

        /* ===== CTA ===== */
        .cta {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            text-align: center;
            padding: clamp(3rem, 8vw, 5rem) 5%;
        }

        .cta h2 {
            font-size: clamp(1.8rem, 4vw, 3rem);
            margin-bottom: 1rem;
        }

        .cta p {
            font-size: clamp(1rem, 2vw, 1.15rem);
            opacity: 0.9;
            margin-bottom: 2rem;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-btn {
            background: transparent;
            color: white;
            border: 2px solid rgba(255,255,255,0.8);
            padding: 0.8rem 2.2rem;
            border-radius: 50px;
            font-weight: 500;
            font-size: 1rem;
            transition: all 0.3s;
            display: inline-block;
        }

        .cta-btn:hover {
            background: white;
            color: var(--primary);
            transform: translateY(-2px);
        }

        /* ===== CONTACT ===== */
        .contact-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: clamp(2rem, 5vw, 4rem);
            align-items: start;
        }

        .contact-info h3 {
            font-size: clamp(1.5rem, 3vw, 2rem);
            color: var(--primary);
            margin-bottom: 1rem;
        }

        .contact-info > p {
            color: var(--text-light);
            margin-bottom: 2rem;
            line-height: 1.7;
            font-size: clamp(0.9rem, 2vw, 1rem);
        }

        .contact-item {
            display: flex;
            align-items: flex-start;
            gap: 1rem;
            margin-bottom: 1.2rem;
        }

        .contact-item i {
            color: var(--accent);
            margin-top: 0.2rem;
            flex-shrink: 0;
        }

        .contact-item h4 {
            font-size: 1rem;
            margin-bottom: 0.2rem;
            color: var(--primary);
        }

        .contact-item p {
            color: var(--text-light);
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .contact-form {
            background: white;
            padding: clamp(1.5rem, 4vw, 3rem);
            border-radius: 20px;
            box-shadow: var(--shadow);
        }

        .form-group {
            margin-bottom: 1.2rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.4rem;
            font-weight: 500;
            color: var(--primary);
            font-size: 0.85rem;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 0.9rem 1rem;
            border: 2px solid #e8e8e8;
            border-radius: 12px;
            font-family: 'Inter', sans-serif;
            font-size: 1rem;
            transition: border-color 0.3s;
            -webkit-appearance: none;
            appearance: none;
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--accent);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }

        /* ===== FOOTER ===== */
        footer {
            background: var(--primary);
            color: white;
            padding: clamp(2.5rem, 6vw, 4rem) 5% 1.5rem;
        }

        .footer-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: clamp(1.5rem, 4vw, 3rem);
        }

        .footer-brand .logo {
            color: white;
            margin-bottom: 1rem;
            font-size: 1.5rem;
        }

        .footer-brand .logo-icon {
            background: white;
            color: var(--primary);
        }

        .footer-brand p {
            color: rgba(255,255,255,0.7);
            line-height: 1.7;
            font-size: 0.9rem;
        }

        .footer-links h4 {
            font-size: 1rem;
            margin-bottom: 1.2rem;
            color: white;
            font-family: 'Inter', sans-serif;
            font-weight: 600;
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.7rem;
        }

        .footer-links a {
            color: rgba(255,255,255,0.7);
            transition: color 0.3s;
            font-size: 0.9rem;
            display: inline-block;
        }

        .footer-links a:hover {
            color: var(--accent);
        }

        .footer-bottom {
            max-width: 1400px;
            margin: 2.5rem auto 0;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            text-align: center;
            color: rgba(255,255,255,0.5);
            font-size: 0.85rem;
        }

        /* ===== MODAL ===== */
        .modal {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.6);
            z-index: 2000;
            align-items: center;
            justify-content: center;
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            padding: 1rem;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            background: white;
            padding: clamp(1.5rem, 5vw, 2.5rem);
            border-radius: 20px;
            max-width: 480px;
            width: 100%;
            position: relative;
            animation: slideUp 0.35s ease;
            max-height: 90vh;
            overflow-y: auto;
        }

        @keyframes slideUp {
            from { transform: translateY(40px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .modal-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: none;
            border: none;
            cursor: pointer;
            color: var(--text-light);
            padding: 0.5rem;
            z-index: 10;
        }

        .modal h3 {
            font-size: 1.6rem;
            color: var(--primary);
            margin-bottom: 0.4rem;
            padding-right: 2rem;
        }

        .modal > p {
            color: var(--text-light);
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
        }

        /* ===== SCROLL TOP ===== */
        .scroll-top {
            position: fixed;
            bottom: 1.5rem;
            right: 1.5rem;
            width: 48px;
            height: 48px;
            background: var(--primary);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: all 0.3s;
            z-index: 999;
            border: none;
            box-shadow: var(--shadow);
            pointer-events: none;
        }

        .scroll-top.visible {
            opacity: 1;
            pointer-events: all;
        }

        .scroll-top:hover {
            background: var(--accent);
            transform: translateY(-3px);
        }

        /* ===== TABLET (768px - 1024px) ===== */
        @media (max-width: 1024px) {
            .hero::before {
                width: 70%;
                border-radius: 0 0 0 100px;
            }

            .gallery-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .footer-container {
                grid-template-columns: 1fr 1fr;
            }
        }

        /* ===== MOBILE (< 768px) ===== */
        @media (max-width: 768px) {
            html {
                font-size: 15px;
            }

            .nav-links {
                position: fixed;
                top: 0;
                right: -100%;
                width: 75%;
                max-width: 320px;
                height: 100vh;
                height: 100dvh;
                background: white;
                flex-direction: column;
                padding: 5rem 2rem 2rem;
                transition: right 0.35s ease;
                box-shadow: -10px 0 40px rgba(0,0,0,0.1);
                align-items: flex-start;
                gap: 1.5rem;
                z-index: 1001;
            }

            .nav-links.active {
                right: 0;
            }

            .nav-links a {
                font-size: 1.1rem;
                padding: 0.5rem 0;
            }

            .nav-links .btn-primary {
                width: 100%;
                margin-top: 1rem;
            }

            .mobile-menu-btn {
                display: block;
            }

            .hero {
                padding: 6rem 5% 3rem;
                min-height: auto;
            }

            .hero::before {
                width: 100%;
                height: 50%;
                top: auto;
                bottom: 0;
                right: 0;
                border-radius: 100px 0 0 0;
                opacity: 0.5;
            }

            .hero-container {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .hero-content {
                order: 1;
                text-align: center;
            }

            .hero-content p {
                margin-left: auto;
                margin-right: auto;
            }

            .hero-buttons {
                justify-content: center;
            }

            .hero-image {
                order: 0;
            }

            .hero-badge {
                bottom: -10px;
                left: 10px;
                padding: 0.8rem 1rem;
            }

            .badge-icon {
                width: 36px;
                height: 36px;
            }

            .badge-text h4 {
                font-size: 0.85rem;
            }

            .badge-text p {
                font-size: 0.7rem;
            }

            .contact-container {
                grid-template-columns: 1fr;
            }

            .contact-info {
                text-align: center;
            }

            .contact-item {
                justify-content: center;
                text-align: left;
            }

            .gallery-grid {
                grid-template-columns: 1fr;
            }

            .gallery-item {
                aspect-ratio: 16/10;
            }

            .footer-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 2rem;
            }

            .footer-brand .logo {
                justify-content: center;
            }

            .footer-links h4 {
                margin-bottom: 0.8rem;
            }

            .footer-links li {
                margin-bottom: 0.5rem;
            }
        }

        /* ===== SMALL MOBILE (< 480px) ===== */
        @media (max-width: 480px) {
            html {
                font-size: 14px;
            }

            .hero-buttons {
                flex-direction: column;
                width: 100%;
            }

            .hero-buttons .btn-primary,
            .hero-buttons .btn-outline {
                width: 100%;
            }

            .services-grid {
                grid-template-columns: 1fr;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }

            .testimonial-author {
                flex-direction: column;
                text-align: center;
            }

            .author-info {
                text-align: center;
            }
        }

        /* ===== TOUCH DEVICE OPTIMIZATIONS ===== */
        @media (hover: none) {
            .service-card:hover {
                transform: none;
            }

            .gallery-overlay {
                opacity: 1;
                background: linear-gradient(to top, rgba(26,58,74,0.7), transparent 70%);
            }
        }

        /* ===== REDUCED MOTION ===== */
        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }

            html {
                scroll-behavior: auto;
            }
        }
    </style>
</head>
<body>

    <!-- Navigation -->
    <nav id="navbar">
        <div class="nav-container">
            <a href="#" class="logo">
                <div class="logo-icon">
                    <i data-lucide="smile"></i>
                </div>
                My Dental Care
            </a>
            <ul class="nav-links" id="navLinks">
                <li><a href="#home" onclick="closeMenu()">Home</a></li>
                <li><a href="#services" onclick="closeMenu()">Services</a></li>
                <li><a href="#why-us" onclick="closeMenu()">Why Us</a></li>
                <li><a href="#gallery" onclick="closeMenu()">Gallery</a></li>
                <li><a href="#testimonials" onclick="closeMenu()">Reviews</a></li>
                <li><a href="#contact" onclick="closeMenu()">Contact</a></li>
                <li><a href="#" class="btn-primary" onclick="openModal(); closeMenu(); return false;">Book Now</a></li>
            </ul>
            <button class="mobile-menu-btn" onclick="toggleMenu()" aria-label="Toggle menu">
                <i data-lucide="menu" size="26"></i>
            </button>
        </div>
    </nav>

    <!-- Mobile Menu Overlay -->
    <div id="menuOverlay" style="display:none; position:fixed; inset:0; background:rgba(0,0,0,0.4); z-index:999;" onclick="closeMenu()"></div>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="hero-container">
            <div class="hero-content" data-aos="fade-up" data-aos-duration="800">
                <h1>Dentistry, <span>differently.</span></h1>
                <p>We are a dental practice designed with you in mind. Using leading-edge technologies and top products to provide the highest quality dental treatments.</p>
                <div class="hero-buttons">
                    <a href="#contact" class="btn-primary">Book Appointment</a>
                    <a href="#services" class="btn-outline">Our Services</a>
                </div>
            </div>
            <div class="hero-image" data-aos="fade-up" data-aos-duration="800" data-aos-delay="150">
                <img src="https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/76f4704d723908555ff8e099698090bb6270d50d.jpg" alt="Happy patient at dental clinic" loading="eager">
                <div class="hero-badge" data-aos="zoom-in" data-aos-delay="400">
                    <div class="badge-icon">
                        <i data-lucide="award"></i>
                    </div>
                    <div class="badge-text">
                        <h4>Excellence Award</h4>
                        <p>Best Dental Studio 2025</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Services Section -->
    <section class="services" id="services">
        <div class="section-header" data-aos="fade-up">
            <span class="label">Our Services</span>
            <h2>Treatments At My Dental Care</h2>
            <p>From improving the health of your smile to creating beautiful cosmetic enhancements, we have you covered.</p>
        </div>
        <div class="services-grid">
            <div class="service-card" data-aos="fade-up" data-aos-delay="0">
                <div class="service-icon">
                    <i data-lucide="scan" size="24"></i>
                </div>
                <h3>Digital Scanning</h3>
                <p>State-of-the-art 3D scanning technology for precise diagnostics and treatment planning without uncomfortable traditional impressions.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
            <div class="service-card" data-aos="fade-up" data-aos-delay="80">
                <div class="service-icon">
                    <i data-lucide="sparkles" size="24"></i>
                </div>
                <h3>Cosmetic Dentistry</h3>
                <p>Transform your smile with veneers, whitening, and aesthetic bonding crafted to enhance your natural beauty.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
            <div class="service-card" data-aos="fade-up" data-aos-delay="160">
                <div class="service-icon">
                    <i data-lucide="shield" size="24"></i>
                </div>
                <h3>Preventive Care</h3>
                <p>Comprehensive cleanings, exams, and personalized oral health plans to keep your smile healthy for life.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
            <div class="service-card" data-aos="fade-up" data-aos-delay="240">
                <div class="service-icon">
                    <i data-lucide="activity" size="24"></i>
                </div>
                <h3>Restorative Dentistry</h3>
                <p>Advanced crowns, bridges, and implants using biocompatible materials that look and feel completely natural.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
            <div class="service-card" data-aos="fade-up" data-aos-delay="320">
                <div class="service-icon">
                    <i data-lucide="align-center-horizontal" size="24"></i>
                </div>
                <h3>Invisalign®</h3>
                <p>Clear aligner therapy for straighter teeth without metal braces. Virtually invisible and completely removable.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
            <div class="service-card" data-aos="fade-up" data-aos-delay="400">
                <div class="service-icon">
                    <i data-lucide="heart-pulse" size="24"></i>
                </div>
                <h3>Emergency Care</h3>
                <p>Same-day appointments for dental emergencies. We prioritize your comfort and provide immediate pain relief.</p>
                <a href="#" class="service-link">Learn more <i data-lucide="arrow-right" size="14"></i></a>
            </div>
        </div>
    </section>

    <!-- Why Choose Us -->
    <section class="why-us" id="why-us">
        <div class="section-header" data-aos="fade-up">
            <span class="label">What Sets Us Apart</span>
            <h2>Why Choose My Dental Care?</h2>
            <p>We strive to deliver an artisan dental experience. Providing exceptional quality care is central to our entire team's effort.</p>
        </div>
        <div class="features-grid">
            <div class="feature-item" data-aos="fade-up" data-aos-delay="0">
                <div class="feature-number">01</div>
                <h3>Patient-First Philosophy</h3>
                <p>You're never rushed, always heard. Every visit is guided by your comfort, lifestyle, and long-term goals.</p>
            </div>
            <div class="feature-item" data-aos="fade-up" data-aos-delay="80">
                <div class="feature-number">02</div>
                <h3>Modern, Minimal Space</h3>
                <p>Designed to soothe, not overwhelm. Our interiors feel more like a curated studio than a traditional clinic.</p>
            </div>
            <div class="feature-item" data-aos="fade-up" data-aos-delay="160">
                <div class="feature-number">03</div>
                <h3>Tech-Driven Precision</h3>
                <p>We pair cutting-edge technology with thoughtful technique to deliver care that's beautifully precise.</p>
            </div>
            <div class="feature-item" data-aos="fade-up" data-aos-delay="240">
                <div class="feature-number">04</div>
                <h3>Luxury in Details</h3>
                <p>From the way you're welcomed to how we follow up, every detail is crafted to feel intentional and personal.</p>
            </div>
        </div>
    </section>

    <!-- Gallery -->
    <section id="gallery">
        <div class="section-header" data-aos="fade-up">
            <span class="label">Our Studio</span>
            <h2>Experience Modern Dentistry</h2>
            <p>Take a look inside our state-of-the-art facility designed for your comfort.</p>
        </div>
        <div class="gallery-grid">
            <div class="gallery-item" data-aos="fade-up">
                <img src="https://kimi-web-img.moonshot.cn/img/static.vecteezy.com/d272f2cf08664f6451ab030aa13971a09d8838d2.jpeg" alt="Modern dental clinic interior" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Treatment Suite</h4>
                </div>
            </div>
            <div class="gallery-item" data-aos="fade-up" data-aos-delay="80">
                <img src="https://kimi-web-img.moonshot.cn/img/ameldental.com/3d1adfb416c39de6fcf0c019b36ee0e58df76d15.jpg" alt="Modern dental equipment" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Advanced Equipment</h4>
                </div>
            </div>
            <div class="gallery-item" data-aos="fade-up" data-aos-delay="160">
                <img src="https://kimi-web-img.moonshot.cn/img/discoverydentalshelby.com/80dafe9c98c7d70d62b9f21163f9f6e9a53981a0.jpg" alt="Digital dental technology" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Digital Technology</h4>
                </div>
            </div>
            <div class="gallery-item" data-aos="fade-up">
                <img src="https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/b2ff60792a4e7e82a68a0c2f1766074de1882b81.jpg" alt="Happy patient smiling" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Happy Patients</h4>
                </div>
            </div>
            <div class="gallery-item" data-aos="fade-up" data-aos-delay="80">
                <img src="https://kimi-web-img.moonshot.cn/img/img.magnific.com/fd8c65082c1998e99f2974050e0056c09378eef0.jpg" alt="Dental examination" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Gentle Examination</h4>
                </div>
            </div>
            <div class="gallery-item" data-aos="fade-up" data-aos-delay="160">
                <img src="https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/8a2703c397028c4b1b24b0cfc0ad2d9505bc2891.jpg" alt="Patient satisfaction" loading="lazy">
                <div class="gallery-overlay">
                    <h4>Patient Care</h4>
                </div>
            </div>
        </div>
    </section>

    <!-- Testimonials -->
    <section class="testimonials" id="testimonials">
        <div class="section-header" data-aos="fade-up">
            <span class="label">Testimonials</span>
            <h2>What Our Patients Say</h2>
        </div>
        <div class="testimonial-slider">
            <div class="testimonial-slide active">
                <div class="quote-icon">
                    <i data-lucide="quote" size="32"></i>
                </div>
                <p>"Excellent dental clinic – the staff are so friendly and extremely knowledgeable. They really go the extra mile with patients, positively encouraging and empowering you to look and feel your best."</p>
                <div class="testimonial-author">
                    <img src="https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/76f4704d723908555ff8e099698090bb6270d50d.jpg" alt="Sarah Mitchell" class="author-avatar" loading="lazy">
                    <div class="author-info">
                        <h4>Sarah Mitchell</h4>
                        <p>Cosmetic Patient</p>
                    </div>
                </div>
            </div>
            <div class="testimonial-slide">
                <div class="quote-icon">
                    <i data-lucide="quote" size="32"></i>
                </div>
                <p>"The most comfortable dental experience I've ever had. The clinic is beautiful, the technology is incredible, and the team makes you feel genuinely cared for."</p>
                <div class="testimonial-author">
                    <img src="https://kimi-web-img.moonshot.cn/img/media.istockphoto.com/b2ff60792a4e7e82a68a0c2f1766074de1882b81.jpg" alt="James Cooper" class="author-avatar" loading="lazy">
                    <div class="author-info">
                        <h4>James Cooper</h4>
                        <p>Invisalign Patient</p>
                    </div>
                </div>
            </div>
            <div class="testimonial-slide">
                <div class="quote-icon">
                    <i data-lucide="quote" size="32"></i>
                </div>
                <p>"I used to be terrified of the dentist until I found My Dental Care. Their gentle approach and modern techniques have completely changed my perspective."</p>
                <div class="testimonial-author">
                    <img src="https://kimi-web-img.moonshot.cn/img/img.magnific.com/fd8c65082c1998e99f2974050e0056c09378eef0.jpg" alt="Emily Zhang" class="author-avatar" loading="lazy">
                    <div class="author-info">
                        <h4>Emily Zhang</h4>
                        <p>Regular Patient</p>
                    </div>
                </div>
            </div>
            <div class="slider-dots">
                <button class="dot active" onclick="goToSlide(0)" aria-label="Slide 1"></button>
                <button class="dot" onclick="goToSlide(1)" aria-label="Slide 2"></button>
                <button class="dot" onclick="goToSlide(2)" aria-label="Slide 3"></button>
            </div>
        </div>
    </section>

    <!-- CTA -->
    <section class="cta">
        <div data-aos="zoom-in" data-aos-duration="700">
            <h2>Your smile journey starts here</h2>
            <p>Experience the difference of modern, patient-centered dentistry. Book your consultation today.</p>
            <a href="#contact" class="cta-btn">Schedule Visit</a>
        </div>
    </section>

    <!-- Contact -->
    <section id="contact">
        <div class="contact-container">
            <div class="contact-info" data-aos="fade-up">
                <span class="label">Get In Touch</span>
                <h3>Visit Our Studio</h3>
                <p>We'd love to welcome you to our practice. Reach out to schedule your appointment or ask any questions.</p>
                
                <div class="contact-item">
                    <i data-lucide="map-pin" size="20"></i>
                    <div>
                        <h4>Location</h4>
                        <p>123 Wellness Boulevard, Suite 400<br>Medical District, NY 10001</p>
                    </div>
                </div>
                <div class="contact-item">
                    <i data-lucide="clock" size="20"></i>
                    <div>
                        <h4>Hours</h4>
                        <p>Mon - Fri: 8:00 AM - 7:00 PM<br>Sat: 9:00 AM - 3:00 PM</p>
                    </div>
                </div>
                <div class="contact-item">
                    <i data-lucide="phone" size="20"></i>
                    <div>
                        <h4>Phone</h4>
                        <p>(555) 123-4567</p>
                    </div>
                </div>
                <div class="contact-item">
                    <i data-lucide="mail" size="20"></i>
                    <div>
                        <h4>Email</h4>
                        <p>hello@mydentalcare.com</p>
                    </div>
                </div>
            </div>
            
            <div class="contact-form" data-aos="fade-up" data-aos-delay="150">
                <h3 style="font-family: 'Playfair Display', serif; font-size: clamp(1.3rem, 3vw, 1.8rem); color: var(--primary); margin-bottom: 1.2rem;">Book Appointment</h3>
                <form onsubmit="handleSubmit(event)">
                    <div class="form-group">
                        <label>Full Name</label>
                        <input type="text" placeholder="John Doe" required>
                    </div>
                    <div class="form-group">
                        <label>Email Address</label>
                        <input type="email" placeholder="john@example.com" required>
                    </div>
                    <div class="form-group">
                        <label>Phone Number</label>
                        <input type="tel" placeholder="(555) 000-0000" required>
                    </div>
                    <div class="form-group">
                        <label>Service</label>
                        <select required>
                            <option value="">Select a service</option>
                            <option>General Checkup</option>
                            <option>Cosmetic Dentistry</option>
                            <option>Invisalign Consultation</option>
                            <option>Emergency Care</option>
                            <option>Teeth Whitening</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>Message</label>
                        <textarea placeholder="Tell us about your dental concerns..."></textarea>
                    </div>
                    <button type="submit" class="btn-primary" style="width: 100%;">Request Appointment</button>
                </form>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-container">
            <div class="footer-brand">
                <a href="#" class="logo">
                    <div class="logo-icon">
                        <i data-lucide="smile"></i>
                    </div>
                    My Dental Care
                </a>
                <p>Modern dentistry designed around your comfort. Experience the difference of patient-centered care with cutting-edge technology.</p>
            </div>
            <div class="footer-links">
                <h4>Quick Links</h4>
                <ul>
                    <li><a href="#home">Home</a></li>
                    <li><a href="#services">Services</a></li>
                    <li><a href="#why-us">Why Us</a></li>
                    <li><a href="#gallery">Gallery</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Services</h4>
                <ul>
                    <li><a href="#">Digital Scanning</a></li>
                    <li><a href="#">Cosmetic Dentistry</a></li>
                    <li><a href="#">Invisalign</a></li>
                    <li><a href="#">Emergency Care</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Legal</h4>
                <ul>
                    <li><a href="#">Privacy Policy</a></li>
                    <li><a href="#">Terms of Service</a></li>
                    <li><a href="#">Accessibility</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p> My Dental Care. All rights reserved. Crafted with care for your smile.</p>
        </div>
    </footer>

    <!-- Booking Modal -->
    <div class="modal" id="bookingModal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal()" aria-label="Close modal">
                <i data-lucide="x" size="22"></i>
            </button>
            <h3>Book Your Visit</h3>
            <p>Fill in your details and we'll contact you to confirm your appointment.</p>
            <form onsubmit="handleModalSubmit(event)">
                <div class="form-group">
                    <label>Name</label>
                    <input type="text" required>
                </div>
                <div class="form-group">
                    <label>Phone</label>
                    <input type="tel" required>
                </div>
                <div class="form-group">
                    <label>Preferred Date</label>
                    <input type="date" required>
                </div>
                <button type="submit" class="btn-primary" style="width: 100%;">Confirm Booking</button>
            </form>
        </div>
    </div>

    <!-- Scroll to Top -->
    <button class="scroll-top" id="scrollTop" onclick="scrollToTop()" aria-label="Scroll to top">
        <i data-lucide="chevron-up" size="20"></i>
    </button>

    <script>
        // Initialize Lucide icons
        lucide.createIcons();

        // Initialize AOS animations
        AOS.init({
            duration: 700,
            once: true,
            offset: 80,
            disable: function() {
                return window.innerWidth < 480;
            }
        });

        // Navbar scroll effect
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 30) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Mobile menu
        const navLinks = document.getElementById('navLinks');
        const menuOverlay = document.getElementById('menuOverlay');
        let menuOpen = false;

        function toggleMenu() {
            menuOpen = !menuOpen;
            if (menuOpen) {
                navLinks.classList.add('active');
                menuOverlay.style.display = 'block';
                document.body.style.overflow = 'hidden';
            } else {
                closeMenu();
            }
        }

        function closeMenu() {
            menuOpen = false;
            navLinks.classList.remove('active');
            menuOverlay.style.display = 'none';
            document.body.style.overflow = '';
        }

        // Close menu on resize to desktop
        window.addEventListener('resize', () => {
            if (window.innerWidth > 768) {
                closeMenu();
            }
        });

        // Testimonial Slider
        let currentSlide = 0;
        const slides = document.querySelectorAll('.testimonial-slide');
        const dots = document.querySelectorAll('.dot');

        function goToSlide(index) {
            slides[currentSlide].classList.remove('active');
            dots[currentSlide].classList.remove('active');
            currentSlide = index;
            slides[currentSlide].classList.add('active');
            dots[currentSlide].classList.add('active');
        }

        // Auto-advance testimonials
        let slideInterval = setInterval(() => {
            const next = (currentSlide + 1) % slides.length;
            goToSlide(next);
        }, 6000);

        // Pause on hover
        const slider = document.querySelector('.testimonial-slider');
        slider.addEventListener('mouseenter', () => clearInterval(slideInterval));
        slider.addEventListener('mouseleave', () => {
            slideInterval = setInterval(() => {
                const next = (currentSlide + 1) % slides.length;
                goToSlide(next);
            }, 6000);
        });

        // Touch swipe for testimonials
        let touchStartX = 0;
        let touchEndX = 0;

        slider.addEventListener('touchstart', (e) => {
            touchStartX = e.changedTouches[0].screenX;
        }, {passive: true});

        slider.addEventListener('touchend', (e) => {
            touchEndX = e.changedTouches[0].screenX;
            handleSwipe();
        }, {passive: true});

        function handleSwipe() {
            const diff = touchStartX - touchEndX;
            if (Math.abs(diff) > 50) {
                if (diff > 0) {
                    goToSlide((currentSlide + 1) % slides.length);
                } else {
                    goToSlide((currentSlide - 1 + slides.length) % slides.length);
                }
            }
        }

        // Modal functions
        function openModal() {
            document.getElementById('bookingModal').classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeModal() {
            document.getElementById('bookingModal').classList.remove('active');
            document.body.style.overflow = '';
        }

        // Close modal on outside click
        document.getElementById('bookingModal').addEventListener('click', (e) => {
            if (e.target === document.getElementById('bookingModal')) {
                closeModal();
            }
        });

        // Close modal on Escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                closeModal();
                closeMenu();
            }
        });

        // Form handlers
        function handleSubmit(e) {
            e.preventDefault();
            const btn = e.target.querySelector('button[type="submit"]');
            const originalText = btn.textContent;
            btn.textContent = 'Sending...';
            btn.disabled = true;
            
            setTimeout(() => {
                alert('Thank you! Your appointment request has been received. We will contact you shortly to confirm.');
                e.target.reset();
                btn.textContent = originalText;
                btn.disabled = false;
            }, 1000);
        }

        function handleModalSubmit(e) {
            e.preventDefault();
            const btn = e.target.querySelector('button[type="submit"]');
            const originalText = btn.textContent;
            btn.textContent = 'Booking...';
            btn.disabled = true;
            
            setTimeout(() => {
                alert('Booking received! We will call you within 24 hours to confirm your appointment.');
                closeModal();
                e.target.reset();
                btn.textContent = originalText;
                btn.disabled = false;
            }, 1000);
        }

        // Scroll to top
        const scrollTopBtn = document.getElementById('scrollTop');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 400) {
                scrollTopBtn.classList.add('visible');
            } else {
                scrollTopBtn.classList.remove('visible');
            }
        });

        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href !== '#') {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        const offset = 70;
                        const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
                        window.scrollTo({ top: targetPosition, behavior: 'smooth' });
                    }
                }
            });
        });
    </script>
</body>
</html>
